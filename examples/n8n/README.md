# n8n integration examples

The first workflow export will be added only after it has been imported and
executed against the supported n8n version. Until then,
`invoice_phase1_node_plan.md` is the authoritative implementation blueprint.

Workflow exports must contain no credentials, API keys, real invoice data,
host-specific URLs, or organization secrets. Use environment variables or n8n
credentials for all endpoints.
